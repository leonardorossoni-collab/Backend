 for(let linha = 0; linha < 8; linha++);{
   let texto = "";
   for(let coluna = 0; coluna <8; coluna++);{
     if ((linha + coluna) texto = % 2 == 0);{
       texto = texto + "#";
     } else {
       texto = texto + "";
     }
   }
   console.log(texto);
 }
